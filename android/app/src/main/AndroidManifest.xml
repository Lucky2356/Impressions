<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Камера нужна для сканирования штрихкодов и съёмки фотографий записей.
         Приложение работает и без неё: код можно ввести вручную. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>

    <!-- Сеть используется только для поиска товара по штрихкоду и проверки
         обновлений; и то и другое отключается в настройках. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Приложение ставится не из магазина, поэтому обновление оно скачивает
         само и отдаёт системному установщику. Без этого разрешения обновиться
         можно было бы только вручную через браузер и «Проводник». Установку всё
         равно подтверждает человек — система спрашивает сама. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>

    <!-- Всё хранится на устройстве, поэтому наружу не уходит ничего: ни в
         облачную копию Google, ни при переносе на новый телефон. Одного
         allowBackup мало — с Android 12 перенос устройство-в-устройство идёт
         мимо него, для этого рядом лежит data_extraction_rules.xml.

         Взамен облака копию делает сам человек: в настройках есть
         «Сохранить копию к себе» и «Восстановить из файла». -->
    <application
        android:label="Впечатления"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Файл обмена: открыть из «Проводника» или прислать через
                 «Поделиться». Раньше присланный файл нельзя было отдать
                 приложению вовсе — приходилось искать его вручную в разделе
                 «Импорт».

                 Расширение нестандартное, поэтому фильтр смотрит на имя файла,
                 а не на тип: MIME у таких файлов система обычно не знает. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="content"/>
                <data android:scheme="file"/>
                <data android:host="*"/>
                <data android:mimeType="*/*"/>
                <data android:pathPattern=".*\\.impressions"/>
                <data android:pathPattern=".*\\..*\\.impressions"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="application/octet-stream"/>
                <data android:mimeType="application/zip"/>
            </intent-filter>

            <!-- Быстрые действия по длинному нажатию на значок. -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts"/>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
