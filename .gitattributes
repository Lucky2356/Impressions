# Скрипты и описания сборок исполняются на linux-раннерах и в Codespaces:
# возврат каретки в конце строки там попадает в саму команду и ломает её.
# Машина разработчика при этом может быть какой угодно.
*.sh text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
